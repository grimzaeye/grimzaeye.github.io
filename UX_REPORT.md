# 웹사이트 UX 및 디자인 자체 평가 보고서 (UX Evaluation Report)

현재 사이트(SSEMS)는 깔끔한 타이포그래피(KoPub 바탕체 등)와 넓은 여백을 활용해 '갤러리'와 '아카이브'로서의 고급스러운 인상을 아주 잘 주고 있습니다. 하지만 방문자가 웹사이트 안에서 길을 잃지 않고 더 오래 머물게 하기 위해(사용성 측면에서) 보완할 수 있는 몇 가지 핵심 지점을 제안합니다.

## 1. 내비게이션 및 흐름 (Navigation Flow)
**문제점:** 
작가님이 지적해주신 부분이 정확합니다. 메인화면이나 Projects에서 특정 작품(상세 글)을 클릭해 들어간 뒤, 다른 작품을 보려 할 때 브라우저의 '뒤로가기'를 누르지 않으면 돌아가기 어렵습니다. 방문자는 한두 번 뒤로가기를 하다가 이탈할 확률이 높습니다.

**개선 제안:**
*   **관련 글(동일 카테고리) 사이드바 배치**: 글 상세 내부의 좌측이나 우측 사이드바 구조를 활용하여, "동일 카테고리(Arts/Research)의 다른 작품 목록"을 5~10개 정도 띄우면 아주 좋습니다.
*   **상하단 '목록으로 돌아가기(Back to List)' 버튼 배치**: 제목 근처나 글 가장 밑단에 `← Arts 목록으로 돌아가기` 형태의 직관적인 텍스트 버튼을 두어 동선을 이어줍니다.
*   **Breadcrumbs (경로 안내) 활성화**: 현 테마에 내장된 기능으로, 글 상단에 `Home > Projects > Arts > 작품명` 처럼 현재 위치를 알려주는 내비게이션을 켜두는 것이 좋습니다.

## 2. 콘텐츠 가독성 (Readability Tools)
**문제점:**
작품 도록이나 리서치 글의 경우 텍스트 길이가 꽤 길어질 수 있습니다. 방문자가 스크롤을 내리다 보면 현재 어떤 파트를 읽고 있는지 잊기 쉽습니다.

**개선 제안:**
*   **자동 목차 (TOC, Table of Contents) 활성화**: `_config.yml`이나 포스트 최상단 설정에 `toc: true`를 켜주면, 본문의 `## 제목` 들만 긁어모아 우측 사이드바에 자동으로 따라다니는 '목차'가 생깁니다. 전문적인 리서치 페이지에서 엄청난 편리함을 줍니다.

## 3. 관련 카테고리/태그 연동 (Discoverability)
**문제점:**
현재 하단에 뜨는 'You may also enjoy(추천 글)'는 사이트 전체의 최신 글을 단순히 나열합니다. Research 글을 읽었는데 갑자기 쿠키 굽는 News가 뜬다면 맥락이 끊깁니다.

**개선 제안:**
*   **추천 알고리즘 변경**: 하단의 추천 글을 '최신 글'이 아닌, `page.category`(같은 카테고리)나 `page.tags`(같은 키워드)를 공유하는 포스트만 뜨도록 로직을 수정하면, 방문자가 관심 있는 주제를 꼬리에 꼬리를 물고 탐색하게 됩니다.
*   **태그(Tag) 클라우드**: 본문 하단에 게시물의 해시태그(`#미디어아트`, `#논문리뷰` 등) 버튼을 띄워, 클릭하면 해당 태그가 달린 글들만 모아주는 태그 모음집 페이지를 하나 만들면 탐색이 훨씬 재미있어집니다.

## 4. 메인 화면의 첫인상 (First Impression Edge)
**문제점 / 개선 제안:**
현재 갤러리는 아주 정갈하지만, 첫 메인 화면 접속 시 시선을 한 번에 압도하는 **강렬한 헤더 이미지 뷰(Hero Banner)**가 있으면 훨씬 웅장한 포트폴리오 사이트가 됩니다.
최근 가장 잘 찍힌 작품 사진 1장을 홈페이지 전체 가로폭으로 꽉 채워서 타이틀(SSEMS) 뒤에 깔아주면, 매우 강렬한 시각적 임팩트를 기대할 수 있습니다.

---
**💡 다음 스텝 제안:**
위 제안 중 마음에 드시는 부분(예: "사이드바에 같은 카테고리 목록 띄우기" 또는 "추천 글 로직 변경하기")을 말씀해 주시면, 바로 코드로 구현해서 사이트에 적용해 드릴 수 있습니다!
