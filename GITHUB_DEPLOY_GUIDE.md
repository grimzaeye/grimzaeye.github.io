# GitHub Pages 배포 가이드 (수정 후 라이브 서버 반영하기)

VS Code 등에서 사이트의 글이나 코드를 수정한 뒤, 그것을 실제 웹사이트(motungeee.com 등)에 라이브로 반영하려면 **터미널(Terminal)** 열고 다음 3단계 명령어를 순서대로 입력하시면 됩니다.

## 기본 배포 3단계 명령어

코드를 수정한 뒤 `Cmd + J` (또는 `Ctrl + \``)를 눌러서 하단의 터미널을 열어주세요.

### 1단계: 변경사항 모두 담기 (장바구니 담기)
```bash
git add .
```
- `.` 은 '변경된 모든 파일'을 의미합니다. 수정한 내역들을 배포 준비 목록에 모두 올리는 과정입니다.

### 2단계: 작업 내역에 이름표 붙여 포장하기 (라벨링)
```bash
git commit -m "여기에 무슨 작업을 했는지 메모를 적으세요"
```
- 예시: `git commit -m "뉴스 포스트 3개 추가 및 오타 수정"`
- 이 단계는 나중에 어떤 작업을 했는지 기록을 남겨두기 위한 목적입니다. 메모는 언제든 자유롭게 한글/영문 섞어서 쓰셔도 됩니다.

### 3단계: GitHub 서버로 쏘아 올리기 (최종 배포)
```bash
git push origin main
```
- 포장된 작업물들을 드디어 원격지(GitHub)로 전송합니다. 
- **이 명령어를 엔터치고 에러 없이 완료되면 배포가 끝난 것입니다.**

> **💡 배포 완료 후 참고사항**
> `git push`가 성공적으로 끝났다고 해서 내 웹사이트에 1초 만에 바로 반영되지는 않습니다. GitHub 환경 내부에서 코드를 조립하는 시간(빌드 시간)이 필요하므로 **보통 1분 ~ 3분 정도 기다린 뒤**, 브라우저에서 '새로고침(F5)'을 하셔야 새로운 내용이 나타납니다.

---

## ⚡️ 가장 많이 쓰는 한 줄 배포 마법 (응용)
위 1~3단계를 매번 따로 치기 귀찮다면, `&&` 기호로 묶어서 한 줄로 칠 수도 있습니다. 현업에서 가장 많이 쓰는 실전용 명령어입니다. 이걸 터미널에 복사-붙여넣기 하셔도 좋습니다.

```bash
git add . && git commit -m "사이트 내용 업데이트" && git push origin main
```
